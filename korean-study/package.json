{
  "name": "korean-study",
  "version": "1.0.0",
  "description": "韓国語学習アプリ（Android / iOS）",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && cp -r index.html conversation.html drill.html vowels.html wordbook.html manifest.json sw.js css data js icons www/",
    "sync:android": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor-community/text-to-speech": "^4.0.0",
    "@capacitor-community/speech-recognition": "^4.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
